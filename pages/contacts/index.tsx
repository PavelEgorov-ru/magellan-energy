import styles from './contacts.module.css';
import { withLayout } from '../../layout/Layout';

const Contacts = () => {
  return <div>страница с контактами</div>;
};

export default withLayout(Contacts);

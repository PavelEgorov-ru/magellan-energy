import styles from './decision.module.css';
import { withLayout } from '../../layout/Layout';
import { Deserializer } from 'v8';

const Decision = () => {
  return <div>страница с решениями</div>;
};

export default withLayout(Decision);

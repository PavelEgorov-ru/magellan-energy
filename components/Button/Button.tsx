import { ButtonProps } from './Button.props';
import cn from 'classnames';
import styles from './Htag.module.css';

export const Button = ({ tag, children, colorWith }: ButtonProps): JSX.Element => {
  return <></>;
};
